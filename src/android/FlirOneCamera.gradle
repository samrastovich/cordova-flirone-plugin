def minSdkVersion = 21

repositories{    
  jcenter()
  flatDir {
    dirs 'src/main/libs'          
 }
}

dependencies {
    compile(name:'androidsdk-release', ext:'aar')
    compile(name:'thermalsdk-release', ext:'aar')

}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}